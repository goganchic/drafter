# API name

# Resource Prototypes

## NotFound

+ Response 404 (application/json)
    + Attributes
        + status: not_found

## InternalError

+ Response 500

# Group Users (NotFound)

Actions of this section should have both common responses

## User [/users] (InternalError)

### List all users [GET]

+ Response 200 (application/json)
    + Attributes
        + users (array)

### User info [GET /users/{id}]

+ Response 200 (application/json)
    + Attributes
        + user (object)
